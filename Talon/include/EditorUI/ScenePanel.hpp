#pragma once
/// @addtogroup UI
/// @{
/**
 * @brief Panel responsible for rendering the active game scene view.
 *
 * This panel typically displays the camera output or rendered scene content.
 */
class ScenePanel {
public:
    /**
	 * @brief Renders the scene panel.
	 *
	 * Displays the game view and any interactive elements needed
	 * for debugging or scene manipulation.
	 */
	void Render();
};
/// @}
